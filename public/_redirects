# Instant edge redirect to whichever project is currently newest
# (lowest `order` value in src/content/works/*.md — currently sediment).
# If you reorder projects so a different one is first, update the
# target here too. src/pages/works/index.astro redirects the same
# way automatically as a fallback, just without the instant edge hop.
/works /works/sediment/ 302
/works/ /works/sediment/ 302
